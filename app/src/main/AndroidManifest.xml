<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.asocialfingers.kachestvo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Utils.Connection.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/kachestvo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/kachestvo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".Tabs.Extra.Pages.Mobile"></activity>
        <activity android:name=".Tabs.Extra.Pages.Desktop" />
        <activity android:name=".Tabs.Extra.Pages.Slider" />
        <activity android:name=".Tabs.Extra.Pages.Holography" />
        <activity android:name=".Admin.PresentationActivity" />
        <activity android:name=".Admin.LoginActivity" />
        <activity android:name=".Admin.AdministrationActivity" />
        <activity
            android:name=".Main.MainActivity"
            android:theme="@style/NoActionBar"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".Beginning.LogoActivity"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>